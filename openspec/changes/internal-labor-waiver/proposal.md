# Internal labor waiver
Use normal service types and identify the collaborator as the customer. Remove origin selection and internal-without-customer behavior from intake. Explicitly waive labor for reason Servicio interno, retaining gross price, discount, net amount, actor and date. Products remain billable and material guides remain unchanged.

Reuse commercial-line discount history and existing apply-discount plus override-discount-limit permissions. No separate internal service type or new accounting ledger. Persist a 100% labor discount with a named rule. Apply during initial quote and commercial revisions. Rollback reverts UI/API changes; existing discount history remains readable.
