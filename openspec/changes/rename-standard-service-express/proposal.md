# Rename Standard service to Express

The business term “Estándar” does not explain that this path skips diagnosis because the requested work is already clear. Present the existing `STANDARD_SERVICE` workflow as “Express” throughout the user interface while preserving its persisted value and behavior.
